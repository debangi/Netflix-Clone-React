import React, { useEffect, useState } from 'react';

const Banner = () => {
  const [movie, setMovie] = useState([]);
  useEffect(() => {}, []);

  return (
    <header>
      {/* <<<<< bg image >>>>>> */}
      {/* title */}
      {/* div > 2 buttons */}
      {/* description */}
    </header>
  );
};

export default Banner;
